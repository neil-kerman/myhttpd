

#include "fastcgi_rnode.hpp"

namespace myhttpd::http {

    void fastcgi_rnode::async_request(std::shared_ptr<request> req, request_handler handler) {

    }

    fastcgi_rnode::fastcgi_rnode(std::string addr, int port) {

    }
}

